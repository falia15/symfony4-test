{% extends 'base.html.twig' %}

{% block title %}Plush {{plush.name}} {% endblock %}

{% block body %}

    <div class="card">
        <div class="card-body">
            <h5 class="card-title">{{plush.name}} </h5>
            <p class="card-text">{{plush.description}}</p>
            <span class="font-weight-bold">Category : {{plush.category.name}}</span>
        </div>
    </div>

{% endblock %}
